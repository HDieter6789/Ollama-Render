services:
  - type: web
    name: ollama-embeddings
    env: docker
    plan: starter
    region: frankfurt
    healthCheckPath: /
    envVars:
      - key: OLLAMA_HOST
        value: 0.0.0.0:11434
    disk:
      name: ollama-models
      mountPath: /root/.ollama
      sizeGB: 10
